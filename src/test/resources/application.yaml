spring:
  application:
    name: server
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    # Placeholder - overridden by Testcontainers @ServiceConnection
    url: jdbc:postgresql://localhost:5432/codingdojo
    username: codingdojo
    password: codingdojo
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
